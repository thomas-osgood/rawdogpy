# Rawdog: Python Client

## Description

This is a Python3 class that defines the Rawdog TCP Communication Protocol client. The class includes functions to both `send` and `recv` data, along with a function that generates and returns a generic metadata dict.
